# torch/transformers pinned: mesh/model_shard.py calls GPT2Block.forward
# directly, and transformers 5.x changed that private call signature.
torch==2.7.0
transformers==4.52.4
pytest
grpcio
grpcio-tools  # only needed to recompile mesh/proto/mesh.proto
